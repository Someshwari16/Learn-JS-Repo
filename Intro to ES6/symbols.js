const gfg = {
    name :"Jansons Institute of Technology",
    desc: "Destination for Holistic Learning."
}

let short_name = Symbol("short_name")
JIT.short_name = "JIT";
console.log(`${JIT.name}, \n${JIT.desc }`);
